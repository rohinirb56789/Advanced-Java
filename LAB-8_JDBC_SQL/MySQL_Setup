Run this SQL command in your MySQL terminal or GUI for coffee DATABASE:

CREATE DATABASE IF NOT EXISTS test;
USE test;

CREATE TABLE IF NOT EXISTS coffee (
    id INT PRIMARY KEY,
    coffee_name VARCHAR(100),
    price FLOAT
);





Run this SQL command in your MySQL terminal or GUI for Employee DATABASE:

CREATE DATABASE IF NOT EXISTS Employee;
USE Employee;

CREATE TABLE IF NOT EXISTS Emp (
    Emp_NO INT PRIMARY KEY,
    Emp_Name VARCHAR(100),
    Basicsalary DOUBLE
);